# 작업 메모 (이수경국어 · 슈국 — 문법 테스트)

## 브랜드
- 폰트: **고운 바탕 + 고운 돋움**(2026-08-29 사용자 확정 "나눔명조를 사용하지 마세요.
  고운 바탕으로 교체" — 저장소 전체 페이지의 세리프를 나눔 명조에서 고운 바탕으로 바꿈.
  고운 바탕은 400·700 굵기만 있어 wght 800 지정은 700으로 표시됨), 색: 세이지 그린 계열
- UI에 컬러 이모지 아이콘을 쓰지 않는다 — 라인 SVG 또는 텍스트만 (사용자 확정 지침, 2026-08-14)

## 문법 테스트 — 어휘 방식으로 개편 (2026-08-28)
어휘 테스트(shueguk-voca)와 같은 구조로 바꿨다: `manifest.js`(카테고리·테스트 등록) +
`data/{코드}-{회차}.json`(문항) + 공용 `test.html`(?c=&r=). 여기에 어휘에는 없는
**배정 기능**(학년/학교/개인)을 얹었다.
- **카테고리 10개**(사용자 지정, 2026-08-28): 음운(pho) · 형태소(mor) · 품사(pos) ·
  단어 형성법(wfm) · 문장(sen) · 높임법(hon) · 사동과 피동(cap) · 시제와 부정 표현(ten) ·
  한글 맞춤법(ort) · 중세 국어(mid)
- **기존 음운 변동 1~10회는 data/pho-1~10.json으로 이전** — 원본 HTML의 QUIZ와 왕복 대조로
  검증(스크래치패드 extract-pho.js). 옛 `shueguk-phonology-N-student.html`은 옛 링크 호환용으로
  남겨 둠(새 링크 발송은 test.html 경유). 나머지 9개 카테고리는 자리만 있고 문항은 비어 있다 —
  **문항 콘텐츠는 임의로 만들지 말 것**(어휘 CLAUDE.md와 같은 지침), 사용자가 주는 자료로 등록.
- **문항 type**: `process`(음운 변동 과정 완성 — 단계마다 변동 종류 select + 형태 입력),
  `short`(단답)/`ox`/`choice`(어휘 test.html과 같은 채점 규칙). process 채점은 옛 페이지와 동일하되
  "기어서/기여서"처럼 빗금 표기 형태는 어느 쪽을 써도 정답으로 인정(formOK).
- **배정**: 결과 시트의 '배정' 탭(A:기록일시 B:대상구분(전체|학년|학교|일부|개인) C:대상 D:카테고리코드
  E:카테고리 F:회차 G:메모 H:상태(진행|마감) I:마감일(yyyy-MM-dd, 선택 — 옛 8열 시트엔 assignSheet_가
  머리글 보충)). 교사 액션(키 필요) assignList/assignAdd/assignSet/
  assignDel(assignSet·Del은 row+time 대조로 행 밀림 방지), 학생 공개 액션 myAssign&grade=&school=&name=.
  학교 대조는 한쪽이 다른 쪽으로 시작하면 인정("화정고"↔"화정고등학교"), 이름·학년은 공백 제거 후 일치.
- **배정 방식 = H WORK과 동일**(2026-08-29 사용자 요청 "h work 와 동일한 배정 방식으로"):
  assign.html 배정 폼이 직접 입력 대신 **공용 학생 선택 위젯**(리포트 저장소
  `student-picker.js/.css` — 전 학년/학년/개인/일부, 리포트 `?action=roster` 재원 명단)을 쓴다
  + **마감(선택, date)·비고(선택)** + [이 학생들에게 배정]. 저장은 종전 assignAdd 그대로(문법
  자체 '배정' 탭 — H WORK처럼 리포트 배정 시트를 쓰지 않음: 학생 조회 myAssign 경로 유지 목적).
  - **대상 형식이 H WORK식으로 확장**(@6 재배포 완료·실배포 왕복 검증): 전체=대상 빈칸(모든 학생),
    학년='고1, 고2' 쉼표 목록, 개인·일부=이름 또는 **동명이인 토큰 '이름|학교|학년'**(위젯이 만듦 —
    myAssign `tokenHit_`이 학교(접두 인정)·학년까지 대조, 학생이 학교·학년을 안 보내면 불일치 처리).
    옛 '학교' 구분은 백엔드 호환 유지(화면에서는 위젯의 일부+학교 필터로 대체).
  - **마감일이 지나면(그 날 밤 11:59까지 유효) myAssign에서 자동 제외** — 교사 목록에는 남는다.
    시트가 날짜로 자동 변환해도 읽도록 `ymd_` 경유. index.html 카드에 '마감 9월 5일 (토)까지' 표시.
  - **제출 현황**: 배정 폼 안 [현황 불러오기] — 배정 대상을 roster로 전개(`expandAssigned`,
    H WORK과 같은 규칙)해 그 회차 결과(unit=카테고리 라벨+round 대조)와 이름+학교(느슨 대조)로
    짝지어 배정/제출/미제출 타일 + 학생 칩(점수 포함) + 배정 외 제출 표시. 동명+동학교는 경고.
  - 검증: 스텁 42건(tools/assign-stub-test.js — 전체/학년 목록/토큰/마감일 제외) + E2E 50건
    (tools/e2e-test.js — github.io 라우트로 위젯을 로컬 서빙, 위젯 조작 → assignAdd 파라미터·현황 대조).
- **제출 게이트**: test.html이 제출 시점에 myAssign으로 배정 여부를 확인해 아니면 막는다.
  확인 실패·옛 배포본(응답에 kind:'assign' 없음)이면 받아준다 — 어휘의 열린 주차 확인과 같은 방침.
  ?preview=1(티쳐스 미리보기)은 확인 생략.
- **결과 제출 payload는 옛 그대로**(time,name,school,grade,unit,round,score,details — unit=카테고리
  라벨) → 결과 수집(doPost)은 재배포 없이도 동작. 옛 데이터의 unit '음운 변동'과 새 '음운'은
  대시보드 단원 필터에서 서로 다른 값으로 보인다(정상).
- **Apps Script 재배포 정보** (clasp 절차는 shuegukweekendtest CLAUDE.md):
  - SCRIPT_ID: `1ZQ7IrLsfMHORsaRCuXqCY1AzqCU4_IjdBFk1WlRvbGcYuWDqPvKnWa3H` (2026-08-29 사용자 제공)
  - DEPLOYMENT_ID: `AKfycbzikteq7gR2ezi_NXc2of8PTvUj5XouJ5ZcZOEWv0YWgjQjs8dZqogQjSr0-LSqneXF`
  - 배정 기능 포함 버전은 2026-08-29 재배포 완료. 티쳐스 페이지는 응답에 kind:'assign'이 없으면
    "재배포 필요" 안내를 띄운다(옛 배포본은 assignList 요청에 결과 rows를 돌려주므로 kind로 구분).
  - `rowSig_`의 \u0001 구분자는 배포본에만 있던 수정을 저장소로 가져온 것 — 덮어쓰지 말 것.
- 검증(2026-08-28): 브라우저 E2E(tools/e2e-test.js — 정적 서빙 + script.google.com 가로채기)로
  응시→채점→배정 차단/허용→제출 payload, index.html 배정 목록, 대시보드 배정 CRUD 왕복 확인.

## 한글 맞춤법(ort) 문항 등록 (2026-08-29, 사용자 요청으로 제작)
- 사용자가 규정 목차를 주고 문항 제작을 명시적으로 요청함: **제1·2장 합쳐 1회(10문항, 총칙·자모
  원문은 사용자가 제공) + 제3~5장 절마다 2회 + 제6장 1회 = 32회차**, 회차마다 OX·선택·단답 혼합
  15문항(1회차만 10문항). data/ort-1~32.json, 문항마다 근거 조항을 explanation으로 표기.
- **단답(short) 채점은 normalize가 공백을 지우므로 띄어쓰기 자체를 단답으로 묻지 말 것** —
  띄어쓰기 문항(제5장)은 선택형(보기에 띄어쓰기 차이) 또는 조사/의존 명사 판별 단답으로 만들었다.
- 검증: `node tools/data-check.js`(형식·정답 범위·process accept 대조 — 데이터 파일 전체),
  브라우저 렌더·채점·payload 확인(스크래치패드 ort-e2e.js), 기존 E2E 28건 회귀 통과.
- 개념 정리(study) 카드는 없음 — 페이지가 바로 테스트 모드로 시작한다. 개념 카드가 필요하면
  data 파일에 study.tabs를 채우면 된다.

## 티쳐스 메뉴 재구성 — 배정하기 / 결과 확인 (2026-08-29 사용자 요청)
- **배정하기 = `assign.html`**(새 페이지): 카테고리 탭 → 회차별 [미리보기]·[배정]·[링크 복사],
  [배정]을 누르면 폼이 열려 학년/학교/개인 대상 저장, 아래 '배정 현황'에서 마감·재개·삭제.
  학생 공통 입구(index.html) 링크 복사도 여기에. 대시보드에 있던 배정 관리는 여기로 옮겼다.
- **결과 확인 = `shueguk-teacher-dashboard.html`**: 결과 전용으로 정리(배정 UI 제거).
- 허브(shueguk-hub gramma.html) 카드도 [배정하기]/[결과 확인]으로 교체. links.html은 옛
  북마크 호환용으로만 남김(허브에서 링크 제거).
- **문법 포인트 컬러 = 연보라**(2026-08-29 사용자 확정 — "개념정리의 진한 녹색을 포인트
  컬러로"는 **진한 녹색 요소를 연보라로 바꿔 달라**는 뜻이었다. 처음에 반대로 읽어 진녹으로
  갔다가 되돌림. 연노랑은 어휘 포인트라 못 씀). **지정 값 = 허브 '학습 관리' 아이콘의
  연보라(t-lav) #EDE4F4 바탕 + #8D83A8**(2026-08-29 사용자 확정 — 출석 색 연보라 #C4A9E4
  세트로 갔던 것도 아니어서 재정정. 임의 값 쓰지 말 것): 칩·선택·버튼 바탕 = #EDE4F4(선택
  테두리 #8D83A8), 강조·진행 바·밑줄·아이콘 = #8D83A8, 옅은 테두리 = #D9CCE8, 연한 바탕 위
  작은 글자만 같은 톤을 어둡게 한 #635A7D(가독성 파생값).
  기본 제출 버튼(전체 제출)·미리보기 링크는 브랜드 녹색 #628C83 유지.
- **배정하기 첫 화면은 카테고리 카드만**(2026-08-29 사용자 요청 "이 페이지에는 음운~중세국어
  버튼만… 버튼이 못생겼어요"): 명조 이름+회차 수 카드 그리드, 문항 없는 카테고리는 '준비 중'
  비활성. 카드를 누르면 회차 화면(#round-view)으로 파고들고 [카테고리로 돌아가기]로 복귀.
- **test.html 템플릿 개선(2026-08-29 사용자 승인 "전부 적용")**: 상단 얇은 진행률 바(테스트
  모드만) + 진행 카드에 미니 바, 학생 정보는 주소로 넘어오면 한 줄 요약으로 접힘([수정]으로
  펼침 — 미입력 제출 시 자동 펼침), 문항 번호 명조+녹색 밑줄, 보기 선택 = 진녹 채움, 개념
  카드 용례는 낱말 나열이면 칩으로(exHtml — ' / ' 줄 구분, 쉼표 나열 3개↑·14자↓·'=' 없을 때만 칩).

## 한글 맞춤법 개념 정리(개념 학습) — 규정을 study 탭으로 (2026-08-29 사용자 요청)
- ort 32개 회차 전부에 `study.tabs`를 넣어 test.html의 **개념 정리 모드**로 규정을 보게 했다
  (어휘 학습 모드와 같은 구조). 카드 = 조항(규정문+용례), note 상자 = 다만·붙임.
  같은 절의 두 회차는 같은 개념 정리를 공유. 생성 스크립트: 스크래치패드 gen-ort-study.js
  (내용은 파일에 들어 있으므로 재실행 불필요 — 수정은 data 파일에서 직접).
- 제1·2장 원문은 사용자가 제공한 텍스트 기준, 나머지는 규정 조항·용례 기준. 원문 전체
  (모든 붙임·용례)로 교체하려면 사용자가 원문을 주면 된다.

## 학생 정보 — 학부모 전화 8자리 + 대상 구분 4가지 (2026-08-29 사용자 요청)
- test.html·index.html 학생 정보에 **학부모님 휴대전화번호 8자리** 칸 추가(= 학생ID, 리포트
  체계와 동일). 개인 페이지 연동 파라미터는 `?p8=` — 이름·학교·학년·p8 넷 다 오면 요약 한 줄로
  접힌다. 제출 payload에 `phone8` 포함, 백엔드 HEADERS에 phone8 열 추가(재배포 @5 완료 —
  결과 시트에 열이 없으면 doPost가 자동 생성). 샘플 이름은 박보검(사용자 지정 — 홍길동 아님).
- ~~배정 대상 구분 = 학년 / 학교 / 일부 학생 / 개인 직접 입력~~ → **H WORK식 학생 선택 위젯으로
  교체**(2026-08-29, 위 '배정 방식 = H WORK과 동일' 절). 저장 ttype '일부'는 그대로(화면 표시
  '일부 학생'), '전체'가 추가됨.
- **학생 개별 페이지(s.html) 연동은 아직 안 붙음** — 리포트 s.html에 문법 카드를 만들 때
  `index.html?name=&school=&grade=&p8=` 형태로 연결하면 된다(어휘 카드의 ?p4= 전달과 같은 방식,
  p8 = 학생ID 8자리 그대로).
