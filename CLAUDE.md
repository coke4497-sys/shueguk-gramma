# 작업 메모 (이수경국어 · 슈국 — 문법 테스트)

## 브랜드
- 폰트: **고운 바탕 + 고운 돋움**(2026-08-29 사용자 확정 "나눔명조를 사용하지 마세요.
  고운 바탕으로 교체" — 저장소 전체 페이지의 세리프를 나눔 명조에서 고운 바탕으로 바꿈.
  고운 바탕은 400·700 굵기만 있어 wght 800 지정은 700으로 표시됨), 색: 세이지 그린 계열
  - 도현체(Do Hyeon, 구글 폰트 — `family=Do+Hyeon`, `font-family:"Do Hyeon"`, 굵기 400 하나)도 브랜드 폰트로 쓴다(2026-09-06 사용자 추가 "고운바탕, 고운 돋움 폰트 사용 지침에 도현체를 추가할게요"). 본문은 종전대로 고운 바탕·고운 돋움, 도현체는 큰 제목·강조 표시 같은 곳에 쓴다 — 어디에 넣을지는 사용자 지시에 따른다.
- UI에 컬러 이모지 아이콘을 쓰지 않는다 — 라인 SVG 또는 텍스트만 (사용자 확정 지침, 2026-08-14)

## 문법 테스트 — 어휘 방식으로 개편 (2026-08-28)
어휘 테스트(shueguk-voca)와 같은 구조로 바꿨다: `manifest.js`(카테고리·테스트 등록) +
`data/{코드}-{회차}.json`(문항) + 공용 `test.html`(?c=&r=). 여기에 어휘에는 없는
**배정 기능**(학년/학교/개인)을 얹었다.
- **카테고리 10개**(사용자 지정, 2026-08-28): 음운(pho) · 형태소(mor) · 품사(pos) ·
  단어 형성법(wfm) · 문장(sen) · 높임법(hon) · 사동과 피동(cap) · 시제와 부정 표현(ten) ·
  한글 맞춤법(ort) · 중세 국어(mid)
- **기존 음운 변동 1~10회는 data/pho-1~10.json으로 이전** — 원본 HTML의 QUIZ와 왕복 대조로
  검증(스크래치패드 extract-pho.js). 옛 `shueguk-phonology-N-student.html`은 옛 링크 호환용으로
  남겨 둠(새 링크 발송은 test.html 경유). 나머지 9개 카테고리는 자리만 있고 문항은 비어 있다 —
  **문항 콘텐츠는 임의로 만들지 말 것**(어휘 CLAUDE.md와 같은 지침), 사용자가 주는 자료로 등록.
- **문항 type**: `process`(음운 변동 과정 완성 — 단계마다 변동 종류 select + 형태 입력),
  `short`(단답)/`ox`/`choice`(어휘 test.html과 같은 채점 규칙). process 채점은 옛 페이지와 동일하되
  "기어서/기여서"처럼 빗금 표기 형태는 어느 쪽을 써도 정답으로 인정(formOK).
- **배정**: 결과 시트의 '배정' 탭(A:기록일시 B:대상구분(전체|학년|학교|일부|개인) C:대상 D:카테고리코드
  E:카테고리 F:회차 G:메모 H:상태(진행|마감) I:마감일(yyyy-MM-dd, 선택 — 옛 8열 시트엔 assignSheet_가
  머리글 보충)). 교사 액션(키 필요) assignList/assignAdd/assignSet/
  assignDel(assignSet·Del은 row+time 대조로 행 밀림 방지), 학생 공개 액션 myAssign&grade=&school=&name=.
  학교 대조는 한쪽이 다른 쪽으로 시작하면 인정("화정고"↔"화정고등학교"), 이름·학년은 공백 제거 후 일치.
- **배정 방식 = H WORK과 동일**(2026-08-29 사용자 요청 "h work 와 동일한 배정 방식으로"):
  assign.html 배정 폼이 직접 입력 대신 **공용 학생 선택 위젯**(리포트 저장소
  `student-picker.js/.css` — 전 학년/학년/개인/일부, 리포트 `?action=roster` 재원 명단)을 쓴다
  + **마감(선택, date)·비고(선택)** + [이 학생들에게 배정]. 저장은 종전 assignAdd 그대로(문법
  자체 '배정' 탭 — H WORK처럼 리포트 배정 시트를 쓰지 않음: 학생 조회 myAssign 경로 유지 목적).
  - **대상 형식이 H WORK식으로 확장**(@6 재배포 완료·실배포 왕복 검증): 전체=대상 빈칸(모든 학생),
    학년='고1, 고2' 쉼표 목록, 개인·일부=이름 또는 **동명이인 토큰 '이름|학교|학년'**(위젯이 만듦 —
    myAssign `tokenHit_`이 학교(접두 인정)·학년까지 대조, 학생이 학교·학년을 안 보내면 불일치 처리).
    옛 '학교' 구분은 백엔드 호환 유지(화면에서는 위젯의 일부+학교 필터로 대체).
  - **마감일이 지나면(그 날 밤 11:59까지 유효) myAssign에서 자동 제외** — 교사 목록에는 남는다.
    시트가 날짜로 자동 변환해도 읽도록 `ymd_` 경유. index.html 카드에 '마감 9월 5일 (토)까지' 표시.
  - ~~제출 현황(배정 vs 제출 대조)~~ — 한때 H WORK처럼 넣었으나 **사용자 "현황 불필요"로 제거**
    (아래 '회차 줄은 [미리보기]+[담기]만' 절). 제출 확인은 '결과 확인' 페이지에서.
  - **배정은 페이지 차원 — 여러 회차 한 번에, 2단계 흐름**(2026-08-29 사용자 "여러개의 회차를
    선택해서" + "카테고리에서 선택해 배정할 테스트를 확정하고 이후 학생 배정의 흐름"):
    assign.html 순서 = **1단계 카테고리 영역(위) → 담은 테스트 이동 바(`#go-assign`) →
    2단계 '학생 배정' 카드(아래, 상시 표시) → 배정 현황**. 회차 줄의 버튼은 [배정]→**[담기]
    토글**(카테고리 섞어 여러 개, `SEL`/`toggleSel`/`renderSelBox`, 칩 ×로 빼기), 회차 화면
    머리줄에 **[전체 회차 담기/빼기]**(`#sec-all`). 배정 = 담은 회차마다 assignAdd를 차례로
    호출(성공한 것만 목록에서 뺌). ~~배정 카드 안 카테고리 드롭다운+회차 버튼~~은 이 흐름으로
    대체(한때 넣었다 뺌 — 선택은 카테고리에서 한다).
  - **회차 줄은 [미리보기]+[담기]만**(2026-08-29 사용자 "링크 복사, 현황 불필요" + "입구 불필요"):
    회차별 링크 복사·[현황] 버튼과 제출 현황 카드(`#st-card`·expandAssigned 대조), '입구' 줄
    (index.html 링크)을 모두 제거. 제출 확인은 '결과 확인' 페이지에서. **'비고'는 화면 표기를
    '메모'로**(저장 파라미터는 memo 그대로) — 메모는 배정 현황 표와 학생 카드에 표시된다.
  - **위젯 색은 이 페이지에서 연보라로 덮어씀**(2026-08-29 사용자 "진초록 버튼 색깔을 변경" —
    공용 student-picker.css는 세이지 그린이라 assign.html `.sp-*` 오버라이드로 t-lav 값 적용.
    공용 CSS는 H WORK 등이 쓰므로 건드리지 말 것).
  - **카테고리 카드 = 2열×5행 + 무채색 라인 아이콘**(2026-08-29 사용자 요청): 아이콘 상자
    `.cico`(회색 #6A716C/#F4F5F3), 준비 중은 더 옅게. ~~카테고리마다 다른 아이콘~~ →
    **전 카테고리 동일한 체크 사각형 하나**(`CAT_ICON` — 2026-08-29 사용자 "모든 아이콘의
    모양을 한글 맞춤법과 동일하게". 카테고리별 아이콘으로 되돌리지 말 것).
  - **디자인 재구성(2026-09-13 사용자 지시서 그대로 적용 — 값은 지시서 수치, 다른 색 토큰·폰트·배경은 무변경)**: ① 회차 목록 2열 그리드(`.list` grid 1fr 1fr, 520px 이하 1열) ② 회차 행 압축(13px 14px·radius 14·border #E5EDE8, 34px 회차 배지 `.rd` → 제목 → 미리보기 → 담기 한 줄) ③ 담기 버튼 = 흰 바탕/#65726B, 담김 = 라벤더 #EDE4F4/#6B5B7B(글자 '담김', ✓ 없음), 담긴 행 `.row.on` 테두리 #D6C9E4 + 배지 라벤더 ④ 상단 카드 좌측 정렬 2단(`.h-left` SHUEGUK+제목 | `.h-right` 1·2 번호 배지 설명 2열, 맨 아래 `.h-foot` 정보 아이콘 줄) ⑤ 카테고리 제목 줄 = 제목 + '전체 n회 · 담김 N'(`.sec-meta`) + [비우기 `#sec-clear`][전체 회차 담기 `#sec-all`] — **옛 담기/빼기 토글은 없앰**(전부 담기면 담기 버튼 비활성, 비우기가 그 카테고리 것만 비움) ⑥ 하단 고정 바 `#selbar`(sticky, '담긴 회차' + `#sb-list` '1회 · 2회' / 7개 이상 'N개 회차' / 없으면 '아직 없습니다', 카테고리가 섞이면 이름을 붙임) + [학생 배정으로 →](`#go-assign`, 비면 `.off` 회색) — 옛 `.gobar` 이동 바 대체 ⑦ 우측 상단 진행 표시 `#steps`(배정하기 › 학생 선택 › 완료, `setStep` — 하단 바 버튼을 누르면 '학생 선택', 배정 성공 뒤 '완료').
  - **배정 현황 줄 선택 + 선택 마감/삭제**(2026-09-13 사용자 "하나하나 눌러서 삭제해야 해서 불편" → "Shift로 범위 선택" 요청): 줄 왼쪽 체크박스(`.a-chk`), 줄 아무 곳 클릭 = 토글, **Shift+클릭 = 마지막으로 누른 줄(`A_ANCHOR`)부터 범위 선택**, Ctrl/⌘는 토글과 같음, 머리글 체크박스 `#a-all` = 전체 선택. 선택이 있으면 표 위 연보라 줄 `#a-selbar`('n개 선택됨' + [선택 마감][선택 삭제][선택 해제]). **선택 삭제는 기존 `assignDel`을 행번호 큰 것부터 차례로 호출**(행이 밀려도 나머지가 어긋나지 않게 — 백엔드 무변경, 재배포 불필요), 선택 마감은 진행 중인 것만 `assignSet`. 선택 키는 `행번호|기록일시`이고 목록을 새로 받으면(`aLoad`) 비운다. 휴대폰은 체크박스·전체 선택으로.
  - 검증: 스텁 42건(tools/assign-stub-test.js — 전체/학년 목록/토큰/마감일 제외) + E2E 60건
    (tools/e2e-test.js — github.io 라우트로 위젯을 로컬 서빙, 담기 2건→한 번에 배정→현황 대조).
- **제출 게이트**: test.html이 제출 시점에 myAssign으로 배정 여부를 확인해 아니면 막는다.
  확인 실패·옛 배포본(응답에 kind:'assign' 없음)이면 받아준다 — 어휘의 열린 주차 확인과 같은 방침.
  ?preview=1(티쳐스 미리보기)은 확인 생략.
- **결과 제출 payload는 옛 그대로**(time,name,school,grade,unit,round,score,details — unit=카테고리
  라벨) → 결과 수집(doPost)은 재배포 없이도 동작. 옛 데이터의 unit '음운 변동'과 새 '음운'은
  대시보드 단원 필터에서 서로 다른 값으로 보인다(정상).
- **Apps Script 재배포 정보** (clasp 절차는 shuegukweekendtest CLAUDE.md):
  - SCRIPT_ID: `1ZQ7IrLsfMHORsaRCuXqCY1AzqCU4_IjdBFk1WlRvbGcYuWDqPvKnWa3H` (2026-08-29 사용자 제공)
  - DEPLOYMENT_ID: `AKfycbzikteq7gR2ezi_NXc2of8PTvUj5XouJ5ZcZOEWv0YWgjQjs8dZqogQjSr0-LSqneXF`
  - 배정 기능 포함 버전은 2026-08-29 재배포 완료. 티쳐스 페이지는 응답에 kind:'assign'이 없으면
    "재배포 필요" 안내를 띄운다(옛 배포본은 assignList 요청에 결과 rows를 돌려주므로 kind로 구분).
  - `rowSig_`의 \u0001 구분자는 배포본에만 있던 수정을 저장소로 가져온 것 — 덮어쓰지 말 것.
- 검증(2026-08-28): 브라우저 E2E(tools/e2e-test.js — 정적 서빙 + script.google.com 가로채기)로
  응시→채점→배정 차단/허용→제출 payload, index.html 배정 목록, 대시보드 배정 CRUD 왕복 확인.

## GitHub Pages 배포가 실패하면 (2026-09-13 겪음)
- 머지 뒤 'pages build and deployment'가 **deploy 단계에서 'Failed to get ID Token'**으로 실패한 적이 있다(빌드는 성공, GitHub 쪽 일시 오류). 실패한 작업 재실행(`rerun_failed_jobs`)을 걸었더니 **14분 넘게 queued에서 안 움직이고 취소도 안 됐다**(409 "re-run that has not yet queued"). 이럴 땐 기다리지 말고 **새 커밋을 main에 올려 배포를 새로 시작**하는 편이 빠르다. 배포 상태는 `actions_list`(list_workflow_runs)로, 실패 원인은 `get_job_logs`로 본다 — 이 환경에서는 github.io 접속이 막혀 있어 화면을 직접 열어 볼 수 없다.

## 음운(pho) — 10회차를 20회차로 나눔 (2026-08-29 사용자 요청 "회차당 문항이 너무 많아 피로도가 크다")
- 문항 내용·번호(1~422)는 그대로 두고 **옛 N회를 반씩 갈라 새 2N-1·2N회**로 만들었다
  (42문항 → 21+21, 옛 10회 44문항 → 19·20회 각 22). 회차마다 21~22문항.
  data/pho-1~20.json · manifest 제목의 번호 범위(예 "음운 변동 실습 (22~42)")도 함께 갱신.
  분할 스크립트는 스크래치패드 split-pho.js — 새 파일의 문항을 이어 붙여 옛 파일과 완전히
  같은지 대조하고 저장한다(재실행 불필요).
- **주의: 이미 나간 배정·링크의 회차 번호 뜻이 바뀌었다** — 옛 pho-3(85~126번)은 이제
  pho-5·6이다. 옛 회차로 배정된 건이 있으면 다시 배정할 것. 결과 시트의 지난 기록은
  옛 번호 그대로 남는다(회차 열이 숫자라 새 기록과 섞여 보인다).
- 옛 `shueguk-phonology-N-student.html`(1~10, 42문항)은 손대지 않았다 — 옛 링크 호환용.
- **12~20회 문항 검토 반영**(2026-09-10 사용자 개선 제안 14건): ① 자음군 단순화 뒤에
  된소리되기(옛 데이터는 된소리되기→자음군 단순화 순) — 257·266·318·338·385·421 + 검토 범위 안의
  같은 유형 247·364도 함께. **1~11회에는 같은 순서의 문항이 8개 남아 있다**(25·35·48·70·92·94·115·219
  — 사용자 결정 대기, 임의로 바꾸지 말 것). ② 놓는(309)은 음끝→비음화 두 단계, 많소(316)는
  자음군 단순화/ㅎ 탈락→된소리되기 두 단계, 닿기(361)는 거센소리되기 한 단계. ③ 같은 규칙이 두
  자리에 걸리면 **한 단계로 한 번에**(327 꽃밭만 음끝 1회, 349 밥맛만 비음화 1회, 332 겉옷은
  음끝 결과 '걷옫'). ④ 연음: 355 돛만이는 연음 단계를 빼고 비음화 결과에 연음을 담아 '돈마니'
  (357 음운론 '으문논'과 같은 방식 — 데이터 전체는 연음을 단계로 두는 문항과 형태에 녹이는
  문항이 섞여 있다. 통일은 사용자 결정 사항). ⑤ 271 살피어는 '살펴'도 정답 — 규칙 목록
  (ruleOptions, 전 회차 동일)에 **'모음 축약'** 추가, accept를 '반모음 첨가/모음 축약'으로.
  규칙과 형태는 따로 채점하므로 '반모음 첨가+살펴' 조합도 정답이 된다(구조 한계).

## 한글 맞춤법 레벨2(ort2) — 조항별 OX 지문형 (2026-09-13 사용자 요청 "한글맞춤법 레벨2 스테이지")
- **자료 = 국어공방 가맹자료 26-9월호 「한글맞춤법 조항반영 OX문제 모음」**(사용자가 다른 세션에서 PDF 57개를
  JSON으로 변환한 zip을 올림 — 조항 57개·OX 3,970문항, 20문항 이하로 나눈 회차 222개, 정답 O·X 절반씩,
  해설 누락 0). 공개 저장소에 올리는 것은 사용자 확인(2026-09-13 "네, 올려 주세요"). 문항의 `id`(HM-005-012)는
  원본 은행 번호 — 원본 대조용으로 남겨 뒀다.
- **새 카테고리 `ort2` '한글 맞춤법 레벨2'**(기존 `ort` 32회는 그대로 레벨1 역할). **배정 페이지 카드는 '한글 맞춤법' 하나**(2026-09-13 사용자 "한글 맞춤법 메뉴 안에 레벨1, 레벨2로"): manifest의 `menu:"ort"`가 붙은 카테고리는 그 카드 안으로 접히고, 회차 화면 위 **레벨 탭**(`.lvl-tab`, `level` 표기 — 레벨1 32회 / 레벨2 222회)으로 고른다. 카드 아래 줄은 '레벨1 32회 · 레벨2 222회', 제목은 '한글 맞춤법 · 레벨2'. 코드(ort/ort2)·라벨·백엔드·학생 화면은 그대로(학생 카드·결과 단원은 여전히 '한글 맞춤법 레벨2'). data/ort2-1~222.json, 회차
  번호는 조항 순서(제1항→제57항). manifest 항목에 **`group`(장 이름)** — 배정 페이지가 이 값으로 회차를
  **장별 접이식(`details.grp`)**으로 묶고 [이 장 담기/빼기] 버튼을 준다(group 없는 카테고리는 종전처럼 한 줄씩).
  담은 칩에는 회차 제목을 붙인다('한글 맞춤법 레벨2 16회 · 제5항 된소리 (3/4)' — 번호만으로는 못 알아봐서).
- **지문형 회차**: 문항이 "원문은 ○○를 …로 제시한다"처럼 **조항 해설 지문을 읽고 푸는 OX**라 지문이 함께
  보여야 한다. data의 `passage:{article,chapter,section,text,paras[]}` → test.html `passageEl(open)` —
  테스트 모드 문항 위 지문 상자(원문 펼침 + 해설은 [해설 펼치기]로 접힘), 개념 정리 탭은 `study.tabs`가
  없어도 지문 전문을 펼쳐 보여 준다(테두리 없음 규칙 그대로). 머리글 배지에 장·절·조항(`subtitle`).
- **해설 지문은 원본 PDF에서 줄바꿈을 살려 다시 만들었다**(2026-09-13, 사용자가 PDF 57개 zip을 올림 — 처음 받은
  zip의 JSON은 convert_ox.py가 줄바꿈을 지워 낱말이 붙어 있었다): 세션 스크래치패드 `reflow.py`(PyMuPDF `get_text('text')`).
  규칙 — PDF 줄은 일정 폭에서 강제로 접히는데 **낱말 경계에서 접히면 줄 끝에 공백이 남고, 낱말 중간이면 없다** → 꽉 찬
  줄(문서 최대 폭의 78% 이상)은 그대로 이어 붙이고(문장 끝 '다.'이면 한 칸 띄움), 짧은 줄 뒤는 띄어서 잇되 **새 줄의 끝
  공백은 지우지 말 것**(지우면 '경음화의규칙성'처럼 붙는다). 번호(1. / (1))·'다만'·'[붙임'·'붙임 1'·'※' 앞은 새 문단.
  **예시 표 = 칸 사이 공백 2칸 이상인 줄**(자모 표 'ㄱ(기역) ㄴ(니은)'만 한 칸) → `{ex:[줄, …]}`로 줄 단위 보존, 칸은 ' · '.
  표 뒤의 공백 없는 짧은 조각('망울]')은 접힌 칸으로 붙이고, '처럼 구별된다.' 같은 산문 조각은 새 문단. 제4항은 '◇ …'로
  시작해 첫 문장을 조항 원문으로. 재변환 결과 조항 원문 57건이 모두 이전 값과 일치. test.html `passageEl`이 문자열은
  `<p>`, `{ex}`는 `.psg-ex`(줄마다 `<span>`)로 그린다. data-check가 두 형식을 검사한다.
- 결과 시트의 unit(단원)은 '한글 맞춤법 레벨2'로 들어가 결과 확인 페이지 단원 필터에 따로 잡힌다. 배정
  백엔드는 cat 코드만 저장하므로 **재배포 불필요**.
- 검증: `node tools/data-check.js`(passage 형식 검사 추가, 274파일·4,867문항) + `NODE_PATH=$(npm root -g)
  node tools/e2e-test.js`(127건 — 카테고리 카드 11개, 레벨2 장별 묶음·이 장 담기·칩 제목·지문 상자·해설 접기·예시 표 줄).

## 문법 스테이지 게임 — 스테이지 맵·플레이 모드·세트 클리어 별·문법 슈스 탑30 (2026-09-15~16 사용자 결정)
사용자가 정한 규칙: **스테이지(=회차) 통과 = 정답률 70%**(다음 스테이지 열림) · **플레이 모드 = 문항마다 한 페이지, 문항당 30초, 답을 고르면 바로 정답** · **스테이지 5개 = 세트, 세트 안을 모두 통과하면 세트 클리어 → 슈퍼스타 별 +1**(회차마다 90% → 별 규칙은 폐지) · **문법 슈스 탑30**은 슈퍼스타 랭킹과 별개, 이름 그대로 표시. 시안은 클로드 디자인 캔버스 '문법 스테이지 게임 시안'(사용자 승인).
- **규칙 한 곳 = `stage.js`**(`STAGE.SET_SIZE=5`·`PASS_PCT=70`·`setsOf(code)`(회차 순서대로 5개씩, 마지막은 남는 만큼)·`setOf`·`progress(code,label,status)`·`nextRound`). index·play·test가 함께 읽는다. **서버 판정은 리포트 저장소 `supabase/migrations/029_gramma_stages.sql`**(gramma_pass_pct()=70, `gramma_sets` 표 = 별 원본, `gramma_submit`이 요청의 `set:{no,rounds}`로 세트 클리어 판정·기록, `gramma_status`에 pass·sets·stars, `gramma_top`) — 세트 구성은 페이지가 정해 보내고 서버는 '그 회차들이 전부 70% 이상인지'만 본다. 값을 바꾸면 양쪽을 함께.
- **index.html = 스테이지 맵**(옛 회차 목록 대체): 단원 카드 → 레벨 탭 → **세트 카드**(`.setcard[data-set]` — 세트 번호·스테이지 범위·장 이름(group)·'별 획득'/'n / 5 클리어'/'앞 세트를 클리어하면 열려요') 안에 **스테이지 노드 5개**(`.node.pass`(초록 체크+최고%)·`.cur`(진한 보라 '도전')·`.open`·`.lock`(자물쇠)). 지금 도전할 스테이지 줄 `.now` + [도전하기] → `play.html?c=&r=&name=&school=&grade=&p8=`. 진행 상태는 `gramma_status` 응답(`STATUS`)을 `STAGE.progress`로 풀어 쓴다 — 정보가 없으면 첫 스테이지만 열림. 머리 카드에 통과 70%·세트 별 +1·[탑30 보기] 타일과 '문법 별 n개 · 세트 n개 클리어'(`#mystars`). `#c=코드` 해시로 오면 그 단원을 바로 연다(play.html의 돌아가기). **정보 칸의 change 이벤트에서 스테이지 맵을 다시 그리지 말 것** — change는 칸을 떠날 때 나므로 [도전하기]를 누르는 순간 버튼이 새로 만들어져 클릭이 사라진다(E2E에서 실제로 잡힘). `stageNote()`로 안내 글만 바꾼다.
- **play.html(새 페이지)**: 시작 화면(스테이지·세트·문항 수·30초·통과 70%, 정보 없으면 시작 잠김, `preview=1`은 기록 없이 응시) → 플레이(상단 고정 `#topbar`: 스테이지 이름·콤보·점수 / 문항 칸 진행 바 `#segs`(초록 정답·붉은 오답·보라 지금) / 30초 줄 `#tbar`+초) → 문항마다 한 화면(큰 글자 `.q-stem` 26px, OX는 큰 버튼 `.big`, 고르기 `.opt`, 단답 `.answer-input`+[확인], 과정형은 test.html과 같은 줄(알약 포함)+[확인], ort2는 [지문 보기] 접이식) → 답을 고르면 즉시 채점·정답 표시 `#fb`(정답/내 답 색, 해설, [다음 문항]) → 결과(`#result`: 클리어!/아쉬워요, %, 점수, 다음 스테이지 열림, `#star-slot` 세트 카드, 최다 콤보·평균 답 시간, 틀린 문항 다시 보기, [다시 도전][다음 스테이지][스테이지 맵으로]). **점수** = 맞히면 100 + 남은 초×2 + 콤보 3부터 20×(콤보−2), 시간 초과 = 오답(`grade(null,true)`). 채점 규칙(normalize·formOK·shortOK)은 test.html과 같다 — **고치면 둘 다**. 기록: 시트 doPost(같은 payload + `mode:'play'`·`points`, details 첫 줄 '플레이 모드 · n점 · 최다 콤보 k') + `gramma_submit`(`mode`·`points`·`set`). 그만두기(×)는 기록 없음.
- **test.html**(전체 제출 방식 — 배정 링크·미리보기용으로 유지): `#star-box`가 '스테이지 통과'/'70% 이상이면 통과'로, `sendStar`가 `mode:'test'`·`set` 정보를 보내고 응답의 set_cleared/set_first/stars로 '세트 n 클리어 · 별 +1' 안내. `stage.js`를 읽는다.
- **top30.html(새 페이지)**: `gramma_top` — 클리어 스테이지 수 → 테스트별 최고 점수 합 순, [전체][중등][고등] + [이번 달](그 달 제출·클리어만), 내 순위 카드(`#me` — 위 순위까지 점수 차), 이름·학교·학년 그대로, 8자리는 응답에 없음. 입구 [탑30 보기] 타일이 학생 정보를 실어 보낸다.
- **슈콩 피드백 연출(2026-09-16 사용자 지시서 그대로 — play.html)**: 정답/오답 패널이 `.fb-panel`(캐릭터 칸 `.fb-avatar` 64px + `.fb-text`의 제목 `.fb-top`·본문 `.fb-body`)로 바뀌었다. **파티클(`fxBurst(host, kind)`)은 캐릭터 칸에만 붙인다**(패널에 붙이면 하트가 글 위로 쏟아져 안 읽힘 — 지시서). 정답 = `assets/sk-heart.png` + 하트 9개(heart), 오답 = `assets/sk-cheer.png` 좌우 흔들림(`fxWobble`) + 꽃 6개(cheer) — **정답이 오답보다 많고 빠르게, 이 비율은 유지**(틀린 게 벌처럼 느껴지지 않게). 별 완성(세트 첫 클리어) = `.star-done`의 118px `.star-stage`(overflow hidden) 안에서만 `assets/sk-best.png` + 노란 반짝이 12개(spark), 아래 '별 하나 완성!' 글 위로 안 넘어감. 파티클은 1회 재생(forwards) 뒤 1.8초에 DOM에서 제거, `prefers-reduced-motion`이면 정지. **img src를 빈 문자열로 두지 말 것**(페이지 주소를 이미지로 다시 요청) — `fxImg`가 실제 값만 넣고 파일이 없으면 `.none`으로 자리만 비운다. 그림은 원장님이 준 2048px 원본을 512px로 줄여 `assets/`에 넣었다(sk-*.png 20장 중 3장만 쓰고 나머지 sk-*·pose-*는 다음 연출용으로 함께 둠, 원본은 `assets/README.md`). 검증: E2E 5c에 4건(오답 꽃 7 span·제거·정답 하트 10 span·별 반짝이 13 span — span 수는 파티클 n + 링 1).
- 검증: `tools/e2e-test.js` 5b(스테이지 맵: 세트 7·노드 32·별 획득·도전·잠김·레벨2 세트 45·정보 없음 안내·play 이동) · 5c(play: 시작 화면·진행 바·30초·큰 글자·바로 정답·점수·콤보 3·결과·gramma_submit/시트 payload·가짜 시계로 시간 초과·세트 클리어 카드·미리보기) · 5d(free=1) · 5e(탑30) — 239건 통과(2026-09-16 기준). 서버 쪽은 리포트 `tools/gramma-sql-test.sh`.

## 티쳐스 참여 현황 `stats.html` (2026-09-16 사용자 요청 "학년 학교 내신반 개인을 선택해 수행률, 점수 등을 확인" + "탑30도 볼 수 있었으면")
- 허브 문법 메뉴 [참여 현황] 카드. **교사 인증 조각**(superstar·hub omr_teacher와 같은 것, 첫 스크립트 자리)으로 수파베이스 표를 직접 읽는다 — `students`(재원만, `LEFT_RE`)·`tt_classes`(book=내신, w* 제외)·`gramma_results`·`gramma_sets`, 1000건씩 끝까지(`sbAll`). **백엔드·마이그레이션 변경 없음.** 학생이 여는 페이지에 이 조각을 넣지 말 것.
- 필터: **대상** [전체/학년/학교/내신반/개인] × **단원**(문항 있는 카테고리, 여러 개 토글) × **기간**(전체/이번 달/최근 7일/30일). 학년은 `normGrade`('2026 고등 1학년'→'고1', 기록의 '고1'과 맞춤). 기록↔명단은 8자리+이름 → 이름 하나뿐 → 동명이인은 학교로(`stuOf`); 내신반 명단 토큰은 앞뒤 괄호를 떼고 정확히 → 끝의 A → 앞 괄호 학교(`stuOfToken`), 못 찾은 이름은 `#cls-hint`로 알린다. 명단에 없는 응시자는 전체·학년·학교 보기에서 '명단 밖' 줄로 합류(내신반·개인에서는 제외).
- 수치: 참여율 = 응시 학생 ÷ 대상, **수행률 = 클리어(스테이지별 최고 ≥70%) ÷ 고른 단원의 전체 스테이지 수**(전체 단원이면 294개라 낮게 나오는 게 정상), 평균 정답률 = 스테이지별 최고의 평균(참여 학생), 플레이 점수 = mode 'play'의 points 합, 별 = gramma_sets 줄 수. 단원별 표 + 학생별 표(정렬 6가지·이름 찾기·80줄씩 더 보기·줄 클릭 → 스테이지 칸·세트). 탑30 탭은 `gramma_top`(authenticated도 실행 권한 있음).
- 디자인은 학생 홈과 같은 규칙(아래). 검증: E2E 12절(가짜 표 응답 — 교사 토큰·전체/내신반/학년/학교/개인·단원·기간·정렬·검색·탑30 탭·디자인 규칙, 21건).

## 학생 홈(index.html) 디자인 개편 + 배정 목록 제거 + 돌아가기 (2026-09-16 사용자 디자인 지시서 6항목 그대로)
- **히어로**: 흰 카드(radius 18, 20px 22px) 왼쪽 정렬(SHUEGUK 10.5px #8B978F .24em · 제목 고운바탕 700 22px #2A332F · 안내 한 문장 "단원을 골라 스테이지를 하나씩 깨요. 정답률 70%를 넘기면 통과, 세트 5개를 다 모으면 별 +1.") + 오른쪽(별 실루엣 76px `#bigstar` — 별 있으면 #FFC21F, 없으면 #F2F5F3/#E2E8E4 · '문법 별 n개' `#mystars` · [탑30 보기] 알약 `#top30-link`). 옛 칩 3개·별 알약·안내 카드는 삭제.
- **학생 정보 요약 한 줄**(`#student-info.collapsed` — 정보가 다 있을 때): `assets/sk-school.png` 44px + '이름 · 학교 학년' + 진행 요약(`#si-sum-sub` — gramma_status로 '통과한 스테이지 n개 · 세트 n개 클리어 · 문법 별 n개') + **[내 진행 확인]**(`#sum-btn`) + [수정](`#si-edit` → 펼침, 펼친 상태에선 `#si-fold` '접기'). 정보가 없으면 입력란 펼침 + [내 진행 확인하기](`#check-btn`, 연보라). **배정 목록(myAssign·`.assign-card`·`#status`·`#list`)은 뺐다**(사용자 "티쳐스 페이지에서 학생 배정 기능을 없앴습니다" → "배정한 테스트 메뉴가 사라진 점을 반영"). 되살리려면 커밋 a5be616의 index.html에서 `check()`/`render(items)`를 가져온다. test.html의 배정 게이트·assign.html은 그대로.
- **단원 카드**(`renderCats` — STATUS가 오면 다시 그린다): 이름 + 별 4개(`.cstars i.on` = 클리어한 세트 수, clip-path 별, #FFC21F/#EEF1EF) + 회차 정보('전체 20회'/'레벨1 10회 · 레벨2 10회') + 진행 막대(#B9A5CC, 통과 스테이지 비율) + 상태('아직 시작하지 않았어요'/'8 / 254 통과'). 체크 아이콘 없음. **준비 중 단원은 `#cats-soon` 한 줄**(자물쇠 + '품사 · 단어 형성법 · …' + '준비 중이에요').
- **색 규칙**(이 두 페이지): 강조·버튼 #EDE4F4/#5A4A6A, hover #E3D7EE, active #D9CAE7, 진행 막대 #B9A5CC, 본문 #2A332F·보조 #4F5C56·캡션 #5C6A64(**#9BA8A1 같은 옅은 회색은 작은 글씨에 쓰지 말 것**), **버튼에 box-shadow 없음**(눌림은 배경색만 — [도전하기]도 연보라, `.node.cur`의 그림자 제거).
- **'내 페이지로' 돌아가기**(`#back-top`, 사용자 "상위 메뉴로 가기 버튼이 없나요?"): 개인 페이지(s.html) 카드가 같은 탭에서 여니까 필요. `?back=`이 있으면 그것, 없으면 **넘어온 주소(document.referrer — 같은 사이트라 `s.html?key=…`까지 온다)**를 sessionStorage `gramma_back`에 기억해 play.html에 다녀와도 유지. 개인 페이지에서 오지 않았으면 숨김.
- E2E 5절을 새 홈 기준으로 다시 씀(요약 줄·수정/접기·카드 별·진행·준비 중 줄·돌아가기 링크 referrer·sessionStorage). **접힌 상태에서는 `#check-btn`이 숨어 있으니 검사는 `#sum-btn`을, 입력은 `#si-edit`로 펼친 뒤** 한다.

## 학생 입구(index.html) — 모든 학생이 자유롭게 응시 (2026-09-14 사용자 요청 "모든 학생들이 문법 테스트 메뉴에 자유롭게 접근")
- **배정 목록 아래 '전체 문법 테스트' 구역**: 배정 페이지(assign.html)와 같은 규칙으로 **단원 카드**(manifest `menu` 단위 — 레벨2는 카드 안, 문항 없는 단원은 '준비 중' 비활성) → **레벨 탭**(`level`) → **회차 줄**(`group`이 있으면 장별 접이식 `details.grp`) → [응시하기]. 배정과 무관하게 등록된 회차는 전부 열린다. 카드는 학생 정보를 넣기 전에도 보인다.
- **[응시하기]는 이름·학교·학년·학부모 휴대전화 8자리가 다 있어야 움직인다**(`readInfo`/`infoOk` — 사용자 조건 "테스트는 본인의 이름과 학교 번호가 채워져 있어야"): 비면 안내 후 학생 정보 카드로 스크롤·빈 칸 포커스, 있으면 localStorage에 기억하고 `test.html?c=&r=&free=1&name=&school=&grade=&p8=`로 이동(배정 카드 링크도 같은 `testUrl` — free 없이).
- **test.html `free=1` = 배정 확인(myAssign) 생략**(preview와 같은 자리, `preview || free`) — 결과 시트 전송·`gramma_submit` 별 판정은 그대로. 선생님이 보낸 링크·배정 카드(free 없음)는 종전대로 배정 확인을 한다.
- 별 표시는 배정 카드와 회차 줄이 공용(`STARS` 캐시 + `markStars(sel, slotSel)` — 키 `단원라벨|회차`, 별을 받은 줄은 번호 배지가 연보라 `.row.star`). 정보가 이미 다 있으면(개인 페이지에서 넘어옴·지난 입력) 페이지가 열릴 때 조용히 받아 둔다.
- 검증: `tools/e2e-test.js` 5b(카드 10장·준비 중·레벨 탭·장별 접이식 222줄·정보 비면 안내·free=1 이동·배정 없이 제출·시트/별 기록) — 162건 통과.

## 오답 확인 화면 — 해설을 층으로 나눔 (2026-08-29 사용자 요청 "가독성 있게")
- 한 문단에 몰아넣던 피드백(`<b>✗ 정답 O</b> 설명…`)을 **판정 줄 → 내 답·정답 줄 → 해설**
  세 층으로 나눴다(`fbTop`/`fbRow`/`fbRows`/`fbWhy`, 스타일 `.fb-*`). 테두리 대신 옅은
  바탕색만 쓰고(정답 #EAF3ED / 오답 #FBEEEB), 판정은 동그란 ✓·✗ 표식으로.
- **음운 변동 과정은 단계마다 한 줄**(`chainHTML` → `.fb-chain`/`.fb-srow`) — 문항의 입력
  줄과 같은 순서·모양(번호 배지 · 변동 종류 칩 · → · 바뀐 형태). 옛 한 줄 화살표 사슬은
  3~4단계에서 줄이 어색하게 끊겨 버렸다. 최종 발음은 연보라 칩으로 강조.
- **단답·과정 문항도 explanation을 보여 준다**(예전엔 OX·선택에만 나왔다). 단답은 다른
  정답도 인정될 때 '○○ 도 정답'을 함께 표기(`altHTML`), 선택형은 번호와 함께 보기 글도 표시.
- **[틀린 n문항만 보기]**(결과 카드, `#review-btn` → `body.only-wrong`) — 21문항을 다시 훑지
  않고 오답만 본다. 다시 누르면 전체로 돌아온다.
- 취소선은 쓰지 않는다(리포트 저장소와 같은 판단 — '다 했다'로 읽힌다). 내 답은 흐린 색으로만.
- **보기 줄에 정오를 직접 표시**(2026-08-29 사용자 "한글 맞춤법도 가독성 높게" 요청으로 추가,
  `markPick`): 채점 뒤 OX·선택형에서 **정답 보기는 초록 + '정답'**, 내가 고른 오답은
  **붉은색 + '내 답'**으로 바뀐다(선택 상태의 연보라는 지운다). 틀린 단답은 입력칸이 붉게
  (`.answer-input.ng`, 맞으면 `.ok`) — 해설을 읽기 전에 눈으로 먼저 보이게 하는 것이 목적.
- **근거 조항은 칩으로 분리**(`splitWhy`/`.fb-art`): 맞춤법 해설 421건 중 196건이 조항만
  ('제5항.'), 134건이 문장 끝의 (제N항)이라 문장과 섞이면 읽기 나빴다. 조항만 있으면 라벨이
  '근거 규정', 문장이 있으면 '해설' + 아래 조항 칩. '붙임 n'·'다만'까지 한 칩으로 잡는다.
  조항이 둘이거나('제14항·제6항') 조항에 말이 붙은 것('제13항의 예외')은 그냥 본문으로 둔다.
  데이터 전체(421건)로 분리 결과를 확인했다 — 해설 원문은 데이터 파일 그대로 두고 표시만 나눈다.

## test.html 화면 재구성 (2026-09-13 사용자 디자인 지시서 6항목 — 수치는 지시서 그대로, 색 토큰·폰트·배경 무변경)
- ① **상단 고정 영역 `#topbar`**(sticky top:0, 바탕 rgba(198,213,210,.96)): 왼쪽 회차 제목(`#h-title`, 고운바탕 700 17px)+작은 글씨 '음운 1회 · 음운 변동 실습 (1~21)'(`#h-round`), 오른쪽 **개념 정리/테스트 모드 알약 토글**(`#mode-tabs` 흰 틀 + 선택 탭 연보라). 아랫줄 `#tb-row2`(테스트 모드에서만) = '완료: n / N'(`#filled-count`) + 진행바(`#pfill`, #B9A5CC) + **[전체 제출] `#submit-btn`**(전 문항 채우면 `.ready` 연보라). 옛 제목 카드·큰 모드 버튼·진행바 카드·화면 위 진행선(`#prog-top`)·화면 아래 고정 제출 바(`.submit-bar`)는 **모두 없앰**. 제출 뒤엔 같은 줄에 '맞힌 문항: n / N'(`#result-display`)만 남고 버튼은 숨김.
- ② **학생 정보 카드 기본 접힘**(`.student-info.collapsed` — 넘어온 정보가 있든 없든): 사람 아이콘 + '이름 · 학년 · 전화번호' 요약(`siSummary` — 빈 항목은 '이름 미입력·학년 미선택·전화번호 8자리 미입력' 회색) + 오른쪽 '수정'. 카드 어디든 누르면 펼침(`.open`, 테두리 #D6C9E4, 글씨 '접기', 요약 줄을 누르면 접힘). 펼치면 2열 그리드(이름/학년, 학교/전화). 제출 때 정보가 비면 `siOpen(true)`. **E2E도 채우기 전에 `#student-info`를 먼저 누른다.**
- ③ 안내 문단 `.section-label` = #EBF0ED 상자 + 정보 아이콘, '(대괄호 없이 한글만)'만 `.dim` 흐리게.
- ④ **과정형 문항 카드 한 줄**(`.q-line`: 번호+짧은 막대 `.q-id` → 단어 `.q-start` → → select → → 입력칸). **단계 줄은 정답 단계 수만큼 미리 만든다**(`FREE_STEPS=false` — 2026-09-13 사용자 "원래대로 몇 단계인지 힌트를 보여주는 것이 좋겠다". 처음 지시서의 [+] 단계 추가 방식은 `FREE_STEPS=true`로 켤 수 있게 남겨 둠: 한 줄로 시작·최대 6줄·마지막 줄 [×]·채점은 줄 수까지 대조). 마지막 줄만 placeholder '최종 발음'. 답을 채운 카드는 `.filled`(테두리 #D6C9E4·막대 #B9A5CC), 채운 입력칸은 `.has`(#FAF7FC). '[발음]' 라벨 제거. 520px 이하는 단계 줄이 다음 줄로 내려간다.
- ⑤ 개념 정리: 분류 탭 = 칩(`.cat-label`, 선택 연보라) + 오른쪽 끝 'N개 규칙'(`#cat-count`, note 제외 수). 카드 = 1px #E5EDE8 테두리, 제목 17px + 분류 태그, 규칙 문장은 왼쪽 2px #DDE6E1 선(`.vocab-meaning`, 회색 상자 없음), 예시는 '예' 라벨 + **전부 칩**(`exHtml` — 쉼표·' / ' 단위, 옛 '3개 이상일 때만 칩' 규칙 폐기).
- ⑥ 연보라 통일: 채움 #EDE4F4 · 테두리 #D6C9E4 · 글자 #6B5B7B · 옅은 채움 #FAF7FC · 진행바 #B9A5CC(옛 #8D83A8·#635A7D·#D9CCE8은 이 페이지에서 전부 교체).
- **아래 '상자 테두리 제거'(2026-08-29) 절은 이 지시로 대체됐다** — 개념 카드·문항 카드·학생 정보 카드는 1px #E5EDE8 테두리가 **지시서 값**이다. E2E의 '테두리 없음' 검사는 새 값 검사로 바꿨다(100건 통과).

## ~~슈퍼스타 별 적립 — 정답률 90% 이상 테스트마다 별 +1~~ (2026-09-13) — **2026-09-16 '세트 클리어 → 별 +1'로 대체됨(위 절)**. 기록·집계 경로는 그대로, 판정만 바뀜.
- **저장**: test.html이 채점 뒤 시트 전송(`sendResult`)과 나란히 **수파베이스 함수 `gramma_submit`**(리포트 저장소 `supabase/migrations/027_gramma_results.sql`)에도 결과를 보낸다(`sendStar` — 이름·학교·학년·8자리·카테고리·회차·점수·정답률). 미리보기(`preview=1`)는 보내지 않는다. 표 `gramma_results`가 **별 집계의 원본**이고 시트는 대시보드용 사본(옛 그대로).
- **판정**: 정답률(반올림) 90% 이상이면 별 1개, **카테고리+회차 단위**(재응시해도 한 번). 함수 응답 `{star, first}`로 결과 카드의 `#star-box`에 '슈퍼스타 별 +1 적립! 내 별에 더해졌어요' / '이미 별을 받았어요' / 90% 미만이면 '90% 이상이면 별 +1' 안내.
- **집계 세 곳(리포트 저장소)**: 학생 페이지 s.html(`student_bundle`의 `gramma_results` 항목 — 028 마이그레이션, `STAR_RULES.gramma=1`, 획득 내역 '문법 테스트 90% 이상'), 슈퍼스타 순위 superstar.html `computeRanking`(gramma_results 직접 조회, 표가 없으면 0), 백엔드 폴백 `collectStars_`/`starRankingData_`(`GRAMMA_SHEET_ID`가 비어 있어 0 — 시트 ID를 넣고 재배포하면 폴백도 센다).
- **입구 페이지 index.html**: 머리 카드에 '정답률 90% 이상이면 슈퍼스타 별 +1 (테스트마다 한 번)' 안내(`.star-rule`), [내 테스트 확인하기] 뒤 `gramma_status`(이름+8자리)로 카드마다 '별 획득 95%' / '최고 60% · 90% 이상이면 별' 배지(`.assign-star`)와 '별을 받은 테스트 n개'.
- 검증: 리포트 `PGHOST=/home/pgtest PGPORT=5499 PGUSER=postgres bash tools/gramma-sql-test.sh`(로컬 PostgreSQL에 001~012+027+028 적용, 판정·first·status·bundle·권한) + 이 저장소 E2E(수파베이스 함수 가로채기 — 5%는 별 없음 안내, 100%는 '별 +1 적립' + 호출 내용, preview 무반응, 입구 배지). **마이그레이션은 실제 수파베이스에 적용해야 동작한다**(리포트 supabase/README.md 'SQL 실행 방법').

## ~~상자 테두리 제거~~ (2026-08-29 사용자 확정 — test.html) — 2026-09-13 디자인 지시로 대체됨(위 절)
- 1차로 개념 카드만 지웠더니 사용자가 "이미지상에는 테두리가 여전히 있다"고 지적 —
  **화면에 남아 있던 나머지도 전부 제거**했다: `.header`·`.mode-label`(모드 탭)·`.cat-label`
  (분류 탭)·`.vocab-card`·`.vocab-example`·`.ex-chip`·`.note-box`·`.student-info`·
  `.progress-bar`·`.final`. 구분은 **옅은 바탕색 + 옅은 그림자**로만 한다(탭 선택 = 연보라
  #EDE4F4 / 연녹 #DCE8E5 바탕). 되돌리지 말 것 — E2E가 `borderTopWidth === '0px'`로 검사한다.
- **남겨 둔 테두리**(뜻이 있어서): 입력 칸(`.answer-input`·`.rule-select`·`.form-input`·
  학생 정보 input/select — 여기에 쓴다는 표시), 문항 카드 `.q-card`의 정답 초록·오답 붉은
  테두리(채점 결과), 버튼 테두리(`.si-edit`·`.review-btn`·`.send-btn.reset`), `<보기>` 상자.

## 한글 맞춤법(ort) 문항 등록 (2026-08-29, 사용자 요청으로 제작)
- 사용자가 규정 목차를 주고 문항 제작을 명시적으로 요청함: **제1·2장 합쳐 1회(10문항, 총칙·자모
  원문은 사용자가 제공) + 제3~5장 절마다 2회 + 제6장 1회 = 32회차**, 회차마다 OX·선택·단답 혼합
  15문항(1회차만 10문항). data/ort-1~32.json, 문항마다 근거 조항을 explanation으로 표기.
- **단답(short) 채점은 normalize가 공백을 지우므로 띄어쓰기 자체를 단답으로 묻지 말 것** —
  띄어쓰기 문항(제5장)은 선택형(보기에 띄어쓰기 차이) 또는 조사/의존 명사 판별 단답으로 만들었다.
- 검증: `node tools/data-check.js`(형식·정답 범위·process accept 대조 — 데이터 파일 전체),
  브라우저 렌더·채점·payload 확인(스크래치패드 ort-e2e.js), 기존 E2E 28건 회귀 통과.
- 개념 정리(study) 카드는 없음 — 페이지가 바로 테스트 모드로 시작한다. 개념 카드가 필요하면
  data 파일에 study.tabs를 채우면 된다.

## 티쳐스 메뉴 재구성 — 배정하기 / 결과 확인 (2026-08-29 사용자 요청)
- **배정하기 = `assign.html`**(새 페이지): 카테고리 탭 → 회차별 [미리보기]·[배정]·[링크 복사],
  [배정]을 누르면 폼이 열려 학년/학교/개인 대상 저장, 아래 '배정 현황'에서 마감·재개·삭제.
  학생 공통 입구(index.html) 링크 복사도 여기에. 대시보드에 있던 배정 관리는 여기로 옮겼다.
- **결과 확인 = `shueguk-teacher-dashboard.html`**: 결과 전용으로 정리(배정 UI 제거).
- 허브(shueguk-hub gramma.html) 카드도 [배정하기]/[결과 확인]으로 교체. links.html은 옛
  북마크 호환용으로만 남김(허브에서 링크 제거).
- **문법 포인트 컬러 = 연보라**(2026-08-29 사용자 확정 — "개념정리의 진한 녹색을 포인트
  컬러로"는 **진한 녹색 요소를 연보라로 바꿔 달라**는 뜻이었다. 처음에 반대로 읽어 진녹으로
  갔다가 되돌림. 연노랑은 어휘 포인트라 못 씀). **지정 값 = 허브 '학습 관리' 아이콘의
  연보라(t-lav) #EDE4F4 바탕 + #8D83A8**(2026-08-29 사용자 확정 — 출석 색 연보라 #C4A9E4
  세트로 갔던 것도 아니어서 재정정. 임의 값 쓰지 말 것): 칩·선택·버튼 바탕 = #EDE4F4(선택
  테두리 #8D83A8), 강조·진행 바·밑줄·아이콘 = #8D83A8, 옅은 테두리 = #D9CCE8, 연한 바탕 위
  작은 글자만 같은 톤을 어둡게 한 #635A7D(가독성 파생값).
  기본 제출 버튼(전체 제출)·미리보기 링크는 브랜드 녹색 #628C83 유지.
- **배정하기 첫 화면은 카테고리 카드만**(2026-08-29 사용자 요청 "이 페이지에는 음운~중세국어
  버튼만… 버튼이 못생겼어요"): 명조 이름+회차 수 카드 그리드, 문항 없는 카테고리는 '준비 중'
  비활성. 카드를 누르면 회차 화면(#round-view)으로 파고들고 [카테고리로 돌아가기]로 복귀.
- **test.html 템플릿 개선(2026-08-29 사용자 승인 "전부 적용")**: 상단 얇은 진행률 바(테스트
  모드만) + 진행 카드에 미니 바, 학생 정보는 주소로 넘어오면 한 줄 요약으로 접힘([수정]으로
  펼침 — 미입력 제출 시 자동 펼침), 문항 번호 명조+녹색 밑줄, 보기 선택 = 진녹 채움, 개념
  카드 용례는 낱말 나열이면 칩으로(exHtml — ' / ' 줄 구분, 쉼표 나열 3개↑·14자↓·'=' 없을 때만 칩).

## 한글 맞춤법 개념 정리(개념 학습) — 규정을 study 탭으로 (2026-08-29 사용자 요청)
- ort 32개 회차 전부에 `study.tabs`를 넣어 test.html의 **개념 정리 모드**로 규정을 보게 했다
  (어휘 학습 모드와 같은 구조). 카드 = 조항(규정문+용례), note 상자 = 다만·붙임.
  같은 절의 두 회차는 같은 개념 정리를 공유. 생성 스크립트: 스크래치패드 gen-ort-study.js
  (내용은 파일에 들어 있으므로 재실행 불필요 — 수정은 data 파일에서 직접).
- 제1·2장 원문은 사용자가 제공한 텍스트 기준, 나머지는 규정 조항·용례 기준. 원문 전체
  (모든 붙임·용례)로 교체하려면 사용자가 원문을 주면 된다.

## 학생 정보 — 학부모 전화 8자리 + 대상 구분 4가지 (2026-08-29 사용자 요청)
- test.html·index.html 학생 정보에 **학부모님 휴대전화번호 8자리** 칸 추가(= 학생ID, 리포트
  체계와 동일). 개인 페이지 연동 파라미터는 `?p8=` — 이름·학교·학년·p8 넷 다 오면 요약 한 줄로
  접힌다. 제출 payload에 `phone8` 포함, 백엔드 HEADERS에 phone8 열 추가(재배포 @5 완료 —
  결과 시트에 열이 없으면 doPost가 자동 생성). 샘플 이름은 박보검(사용자 지정 — 홍길동 아님).
- ~~배정 대상 구분 = 학년 / 학교 / 일부 학생 / 개인 직접 입력~~ → **H WORK식 학생 선택 위젯으로
  교체**(2026-08-29, 위 '배정 방식 = H WORK과 동일' 절). 저장 ttype '일부'는 그대로(화면 표시
  '일부 학생'), '전체'가 추가됨.
- **학생 개별 페이지(s.html) 연동은 아직 안 붙음** — 리포트 s.html에 문법 카드를 만들 때
  `index.html?name=&school=&grade=&p8=` 형태로 연결하면 된다(어휘 카드의 ?p4= 전달과 같은 방식,
  p8 = 학생ID 8자리 그대로).

## 문법 지식 자료 — `knowledge/` (2026-09-13 구축, 사용자 요청 "클로드가 문법을 학습하도록 자료를 주고 싶다")
클로드는 세션마다 새로 시작하므로 '학습' 대신 **문항을 만들거나 검토할 때 반드시 먼저 읽는 참고서**를 저장소에 둔다.
- **`knowledge/출제지침.md`(공통) + 카테고리 문서를 끝까지 읽은 뒤에만 문항을 만든다.** 문서에 없는 규칙은 쓰지 않고 사용자에게 자료를 요청한다.
  검수에서 정해진 것은 그 문서의 '결정 사항'에, 못 정한 것은 '미결 사항'에 적는다 — **미결 사항을 임의로 정해 데이터를 고치지 말 것.**
- 지금 있는 문서: 음운 3편(`pho-01-음운체계`·`pho-02-음운변동`·`pho-03-기출유형`). 출처는 사용자가 드라이브에 올린 「다담문법」 4-1·4-2 PDF —
  이 환경은 드라이브 직접 다운로드(drive.google.com)가 네트워크 정책으로 막혀 있어 **드라이브 연결(MCP)의 OCR 텍스트**로 읽었다. OCR이 자음 글자를
  깨뜨리므로(ㄱ→기, ㄷ→亡, ㅎ→공…) 규칙 문장은 학교 문법 표준 표현으로 복원해 적고 예시는 온전한 것만 썼다. 기출 문항 본문은 옮기지 않는다(시판 교재).
  드라이브 폴더에는 5-1 한글 맞춤법·5-2 표준어 규정 PDF도 있다(아직 정리 안 함). 나머지 8개 카테고리는 사용자가 자료를 주면 같은 형식으로.
- **`node tools/pho-audit.js`** — 음운 process 문항의 단계별 정합성 검사(한글 자모를 분해해 고른 규칙과 실제 형태 변화가 맞는지·최종 발음의 받침이
  7개 대표음인지·미결 순서 유형 건수·start 중복). 같은 규칙이 여러 자리에 동시에 걸린 문항(9/10 결정 ⑦)은 통과시키고, 규칙 결과에 연음을 녹인 단계·ㄾㄼㄺ→ㄹ 뒤의 된소리되기·ㄴ 첨가와 끝소리 규칙의 자리 순서 위반은 오류로 잡는다.
  `KNOWN`에 적힌 문항은 '사용자 결정 대기'로만 표시(종료 코드 0) — 결정이 나면 데이터를 고치고 그 항목을 지운다. **음운 문항을 만들거나 고치면 반드시 돌릴 것.**
- **형태소 문서 `mor-01-형태소.md`**(2026-09-13, 사용자가 올린 PDF 5개 — 슈국 자체 자료 3개(문법 매뉴얼 1-1 TEST·노베8 08·튜토리얼 STAGE 01)와 교재 ①형태소·수능형 (31)): 개념·분류·이형태·분석 규칙·**자료 정답 그대로의 분석 사례집**·문항 유형. **결정**: '이다' = 서술격 조사 어간 '이-' + 종결 어미 '-다'(형태소 2개, 자료 ③④는 하나로 적어 두었으니 그대로 옮기지 말 것). '했다' = 하-/-였-/-다, '-십시오' = -시-/-ㅂ시오(같은 날 사용자 결정, 자료 ④와 다름). 수능형 2번(정답 없음)은 옮기지 않고, 교재 ②·수능형 ⑤의 기출·타사 문항도 공개 저장소에 옮겨도 된다(같은 날 사용자 결정). 미결 없음.
- **형태소 줄의 실질/형식·자립/의존은 드롭다운 대신 연보라 알약 버튼**(2026-09-14 사용자 "드롭다운 선택이 이상해요 — '실질/형식·실질·형식' 세 줄의 차이가 드러나도록, 선택해야 하는 옵션에는 테두리 없는 연보라 알약"): 기본 드롭다운은 안내 글('실질/형식')과 선택지 두 개가 같은 모양의 세 줄로 보여 셋 중 하나를 고르는 것처럼 읽혔다. test.html `selHTML` — **선택지가 `PILL_MAX`(3)개 이하면** `select`를 숨기고(`.rule-select.hid`, 값 저장·채점·진행 카운트는 종전 그대로) `.pills[data-for=selectId]`(작은 안내 글 `.pl-lab` + `.pill` 버튼)를 그린다. 알약 = 테두리 없음·#EDE4F4/#6B5B7B, 선택 = #6B5B7B 채움에 흰 글자(hover에도 유지), 같은 알약을 다시 누르면 해제. 클릭은 `#questions` 위임 → select 값 + change 이벤트. 채점 뒤 `pillSync(sel)`이 묶음에 ok/ng/done(잠금)/none(안 고름 — 안내 글 붉게)을 붙인다. 음운 변동 종류(15개)는 드롭다운 그대로. 휴대폰(≤520px)은 알약 두 묶음이 빈칸 아래 줄로 내려간다(`.pillset` 100%). 검증: E2E 3c(알약 묶음·색·선택/해제·진행 카운트·정오 표시·음운 드롭다운 유지) — 173건 통과.
- **형태소 문항 = 레벨1 `mor-1~10` · 레벨2 `mor2-1~10`, 회차 15문항**(2026-09-13 사용자 "'귀여워'를 형태소로 나누시오 → 귀엽-(실질, 의존), -어(형식, 의존) 같은 문항을 많이" + "레벨마다 15문항 × 10회차"). 배정 카드는 '형태소' 하나에 레벨 탭(manifest `mor` 레벨1 / `mor2` 레벨2 `menu:"mor"` — 맞춤법과 같은 구조). **`tools/mor-gen.py`가 data를 만든다**(단어·문장 은행 + 단답 은행이 그 안에 있음 — 문항을 고칠 땐 데이터 파일이 아니라 생성기를 고치고 다시 돌릴 것). 나누기 문항은 `process` 재사용: 형태소마다 종류 드롭다운(실질·자립/실질·의존/형식·의존) + 형태 입력, **붙임표는 빈칸 앞뒤에 표시**(`processUI.hyphenMarks` — step form의 앞/뒤 '-'를 보고 `.hy` 표시, 어간 `[ ]-`·어미/접미사 `-[ ]`·선어말 `-[ ]-`·조사/자립은 없음. 2026-09-13 사용자 "조사를 제외한 의존 형태소는 - 표시가 필요합니다" → "이걸 표시해주세요. 빈칸 앞뒤로"). 학생은 형태소만 쓰고 채점은 앞뒤 붙임표를 뗀 뒤 대조(formOK의 bare). 다른 모양 붙임표(－‐–)는 normForm이 '-'로 통일. 정답 표시는 `processUI.firstForm`으로 대표 표기만. **test.html `processUI`**(파일 단위 — select/select2/mid/last/arrow/sep/chainTitle/startLabel/brackets/formFirst/freeSteps/maxSteps)로 과정형 문구·배치를 바꿨고 기본값은 음운 변동 그대로. **형태소 줄 = 빈칸(형태소) → [실질/형식] → [자립/의존] 두 드롭다운**(2026-09-13 사용자 "형태소의 개수만큼 빈칸을 주고 그 빈칸에 해당하는 형태소의 종류를 각각 실질/형식, 자립/의존 중 선택"): 파일의 `ruleOptions2`가 있으면 둘째 드롭다운이 생기고 step의 `accept2`로 채점(둘 다 맞아야 정답, 틀린 쪽만 붉게), `formFirst:true`면 입력칸이 앞. 빈칸은 형태소 수만큼 미리 만든다(`freeSteps:false` — 한때 [+]로 늘리는 방식으로 갔다가 사용자 지정으로 되돌림. freeSteps 옵션 자체는 남아 있음). data-check가 accept2 ∈ ruleOptions2를 검사한다. 자료에 없는 단어·문장은 생성기에 '※' 표시(사용자 검수 대상, 목록은 mor-01 §8). E2E는 미등록 안내 검사를 형태소→품사로, 비활성 카드 8→7로 바꿈.
- **결정된 것(2026-09-13 사용자)**: ① **겹받침+된소리 순서 = "된소리되기는 조건이 살아 있는 시점에"** — 자음군 단순화로 조건 자음이 떨어지는 단어(ㄾ·ㄼ→ㄹ·ㄱ 앞 ㄺ→ㄹ)는 된소리되기가 먼저, 남는 자음이 조건이면 순서 무관(`pho-02` §7 ③). 247·318·338·421을 바꾸고 267을 나눔.
  ② **연음은 규칙이 아니지만 항상 별도 단계**(13문항 분리). ③ **ㄴ 첨가 ↔ 끝소리 규칙 = 끝소리 규칙이 걸리는 자음의 위치**(앞말 끝이면 ㄴ 첨가보다 먼저, 뒷말 끝이면 뒤 — 기존 7문항 그대로). 같은 날 입버릇 46의 1단계 오타('입뻐룻'→'입뻐릇')도 고침.
- ④ **꽃잎 246·나뭇잎 310은 끝소리 규칙을 두 단계로**(앞말 끝 → ㄴ 첨가 → 뒷말 끝, 사용자 선택). ⑤ **같은 규칙이 두 번 이상 나오는 문항 7건에 `hint`·`explanation`**(사용자 요청 "설명과 힌트") — test.html이 모든 type의 `q.hint`를 문항 머리 아래 연보라 '힌트' 상자로 보여 준다(`.q-hint`). 새 문항도 같은 원칙(출제지침 §1).
- **미결 사항(`pho-02-음운변동.md` 맨 아래)**: ① ~~꽃잎·나뭇잎~~ → 결정됨(위 ④) ② ~~한 단계에 두 규칙을 뭉친 7건~~ → **그대로 두기로 결정**(사용자 "그대로 둡니다" — 닦달하다 379의 비표준 발음을 알린 뒤의 결정. `pho-audit.js` KEEP 목록, 새 문항은 한 단계 = 한 규칙).
